<!-- Thanks for contributing! Keep PRs focused on one logical change. -->

## What does this change?

<!-- A short description of the change and why. -->

## How was it tested?

<!-- e.g. ran `npm run lint && npm run build && npm test -- --run`; tested squad mode against a local relay; switched languages and checked X. -->

## Checklist

- [ ] `npm run build` passes (type-check + build)
- [ ] `npm test -- --run` passes
- [ ] `npm run lint` passes with no errors (required CI check)
- [ ] For translation changes: only edited existing keys (no keys missing from `en.json`)
